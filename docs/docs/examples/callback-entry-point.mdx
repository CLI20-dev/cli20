---
sidebar_position: 7
title: 6. Callback Entry Point
---

import {ExampleSource} from '@site/src/components/ExampleDoc';
import {ExamplePlayground} from '@site/src/components/ExamplePlayground';

# Callback Entry Point

Open this when parsing should populate application state for some other entry
point, service object, or orchestration function.

This page is the bridge between cli20 and a larger application architecture.
The parser gathers state first, and then your real program entry point runs with
that prepared state.

## Source

<ExampleSource
  example={{
    sourcePath: 'examples/callback_entry_point.cc',
  }}
/>

## What This Example Shows

This example updates application state incrementally as each argument is parsed
and then hands the finished state to the real entry point. It keeps CLI wiring
at the edge, supports repeated `--include` options without swallowing
positionals, and leaves the final state in `verbose`, `jobs`, `includes`, and
`inputs`.

## Try It

<ExamplePlayground name="callback_entry_point" defaultArgs="--verbose --jobs 8 --include src --include include main.cc lib.cc" />

Things to try:

- **Happy path** — `--verbose --jobs 8 --include src --include include main.cc lib.cc`
- **Help** — `--help`
- **Multiple includes** — add more `--include path` flags to see the list grow
- **No inputs** — `--verbose` to see what happens with no positional arguments

## Related Examples

- [Bind](/docs/examples/bind)
- [Subcommands](/docs/examples/subcommand)
